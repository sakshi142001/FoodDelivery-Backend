name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:

  build-user-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build JAR (user-service)
        run: mvn clean package -DskipTests
        working-directory: user-service

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/user-service ./user-service

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/user-service


  build-order-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build JAR (order-service)
        run: mvn clean package -DskipTests
        working-directory: order-service

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/order-service ./order-service

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/order-service


  build-eureka-server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build JAR (eureka-server)
        run: mvn clean package -DskipTests
        working-directory: eureka-server

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/eureka-server ./eureka-server

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/eureka-server


  build-api-gateway:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build JAR (api-gateway)
        run: mvn clean package -DskipTests
        working-directory: api-gateway

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/api-gateway ./api-gateway

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/api-gateway